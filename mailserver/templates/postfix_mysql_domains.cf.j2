user={{ postfix_sql_user }}
password={{ postfix_sql_password }}
dbname={{ postfix_sql_dbname }}
table=domains
select_field=domain
where_field=domain
hosts=127.0.0.1
additional_conditions = and enabled = 1